var style = {
  color: "#ffaaaa",
  };

var User = React.createClass({
      render: function () {
        return (
         <div className="users" >
           
           </div>

        )
    }
});

ReactDOM.render(
  <User/>,
  document.getElementById('users')
    );
